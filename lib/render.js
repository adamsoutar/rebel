// There's no need to fill out the entire tree
// recursively. We can render & match up state as
// we go.

// Sends the result of "createElement" to DOM
export default function render (el, domTarget) {

}
